version: '3.4'

services:
    app:
        restart: always
        build: 
            dockerfile: dockerfile
            context: ./app
        ports: 
            - '8080:80'
    api:
        restart: always
        build: 
            dockerfile: dockerfile
            context: ./api
        ports:
            - '8081:80'